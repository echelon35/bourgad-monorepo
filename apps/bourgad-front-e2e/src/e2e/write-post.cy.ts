describe('Write post', () => {

  it('should access homepage not connected', () => {
    /**
     * This test checks if the homepage is accessible from visitor.
     */
  });

  it('should not access the write post component', () => {
    /**
     * This test checks that the write post component is not accessible when the user is not connected.
     */
  });

  it('should access connection page', () => {
    /**
     * This test checks that the visitor can be connected.
     */
  });

  it('should open the write post component', () => {
    /**
     * This test checks if the user can open the write post component after being connected.
     */
  });

  it('should post successfully', () => {
    /**
     * This test checks if the user can successfully post a new post.
     */
  });
})